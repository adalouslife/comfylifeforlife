{
  "tests": [
    {
      "name": "smoke_health_check",
      "input": { "op": "health_check" },
      "timeout": 15000
    },
    {
      "name": "smoke_ping_alias",
      "input": { "op": "ping" },
      "timeout": 15000
    }
  ],
  "config": {
    "cpuFlavor": "CPU-1C-2GB",
    "env": [
      { "key": "RUNPOD_TEST_MODE", "value": "true" }
    ]
  }
}
