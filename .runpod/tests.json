{
  "tests": [
    {
      "name": "smoke_test",
      "input": {
        "op": "ping"
      },
      "timeout": 10000
    }
  ],
  "config": {
    "cpuFlavor": "CPU-2C-4GB",
    "env": [
      { "key": "RUNPOD_TEST_MODE", "value": "true" }
    ]
  }
}
