{
  "tests": [
    {
      "name": "smoke_gpu_online",
      "input": { "op": "health_check" },
      "timeout": 20000
    }
  ],
  "config": {
    "runsOn": "GPU",
    "gpuCount": 1,
    "env": [
      { "key": "RUNPOD_TEST_MODE", "value": "true" }
    ]
  }
}
